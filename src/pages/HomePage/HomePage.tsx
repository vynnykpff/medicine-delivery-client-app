import { Container, ShopsList } from '@components';

type Props = {};

export const HomePage = (props: Props) => {
  const {} = props;

  return (
    <Container>
      <ShopsList />
    </Container>
  );
};
